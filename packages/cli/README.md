# oxbowui

CLI for installing Oxbow blocks into Astro projects.

## Usage

```bash
npx oxbowui list
npx oxbowui add hero 1
npx oxbowui add pricing 3
```

### Options

- `--cwd <path>`: target project directory
- `--yes`: overwrite existing files
- `--all`: install all blocks
