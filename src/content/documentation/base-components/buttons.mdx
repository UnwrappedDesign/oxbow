---
title: Buttons
description: Copy-paste all button variants, sizes, and states in one place.
order: 1
category: Base components
---

import Tabs from "@/components/fundations/tabs/Tabs.astro";
import TabButton from "@/components/fundations/tabs/TabButton.astro";
import TabPanel from "@/components/fundations/tabs/TabPanel.astro";
import Button from "@/components/fundations/buttons/Button.astro";
import Plus from "@/components/fundations/icons/Plus.astro";
import ArrowRight from "@/components/fundations/icons/ArrowRight.astro";
import SearchIcon from "@/components/fundations/icons/Search.astro";
import AlertTriangle from "@/components/fundations/icons/AlertTriangle.astro";

## Button components

Free and open-source Tailwind CSS button components built for modern applications and websites. 
These buttons are built using HTML and Tailwind CSS.



### Core palette

<Tabs defaultTab="preview">
  <TabButton slot="tabs" tab="preview">Preview</TabButton>
  <TabButton slot="tabs" tab="code">Code</TabButton>

  <TabPanel slot="content" tab="preview" class="p-4 bg-base-50 dark:bg-white/5 rounded-xl">
    <div class="grid gap-3 sm:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4">
      <Button>Default</Button>
      <Button variant="accent">Accent</Button>
      <Button variant="accentDark">Accent Dark</Button>
      <Button variant="muted">Muted</Button>
      <Button variant="alternative">Alternative</Button>
      <Button variant="outline">Outline</Button>
      <Button variant="ghost">Ghost</Button>
      <Button variant="text">Text</Button>
      <Button variant="link">Link</Button>
    </div>
  </TabPanel>

<TabPanel slot="content" tab="code">
```astro
<Button>Default</Button>
<Button variant="accent">Accent</Button>
<Button variant="accentDark">Accent Dark</Button>
<Button variant="muted">Muted</Button>
<Button variant="alternative">Alternative</Button>
<Button variant="outline">Outline</Button>
<Button variant="ghost">Ghost</Button>
<Button variant="text">Text</Button>
<Button variant="link">Link</Button>
```
  </TabPanel>
</Tabs>



### Semantic statuses

<Tabs defaultTab="preview">
  <TabButton slot="tabs" tab="preview">Preview</TabButton>
  <TabButton slot="tabs" tab="code">Code</TabButton>

  <TabPanel slot="content" tab="preview" class="p-4 bg-base-50 dark:bg-white/5 rounded-xl">
    <div class="grid gap-3 sm:grid-cols-2 lg:grid-cols-4">
      <Button variant="info">Info</Button>
      <Button variant="success">Success</Button>
      <Button variant="warning">Warning</Button>
      <Button variant="danger">Danger</Button>
    </div>
  </TabPanel>

  <TabPanel slot="content" tab="code">
```astro
<Button variant="info">Info</Button>
<Button variant="success">Success</Button>
<Button variant="warning">Warning</Button>
<Button variant="danger">Danger</Button>
```
  </TabPanel>
</Tabs>

### Size scale

<Tabs defaultTab="preview">
  <TabButton slot="tabs" tab="preview">Preview</TabButton>
  <TabButton slot="tabs" tab="code">Code</TabButton>

  <TabPanel slot="content" tab="preview" class="p-4 bg-base-50 dark:bg-white/5 rounded-xl">
    <div class="grid gap-3 sm:grid-cols-2 lg:grid-cols-3">
      <Button size="xxs">Size xxs</Button>
      <Button size="xs">Size xs</Button>
      <Button size="sm">Size sm</Button>
      <Button size="base">Size base</Button>
      <Button size="md">Size md</Button>
      <Button size="lg">Size lg</Button>
      <Button size="xl">Size xl</Button>
    </div>
  </TabPanel>

  <TabPanel slot="content" tab="code">
```astro
<Button size="xxs">Size xxs</Button>
<Button size="xs">Size xs</Button>
<Button size="sm">Size sm</Button>
<Button size="base">Size base</Button>
<Button size="md">Size md</Button>
<Button size="lg">Size lg</Button>
<Button size="xl">Size xl</Button>
```
  </TabPanel>
</Tabs>

### Icon layouts

<Tabs defaultTab="preview">
  <TabButton slot="tabs" tab="preview">Preview</TabButton>
  <TabButton slot="tabs" tab="code">Code</TabButton>

  <TabPanel slot="content" tab="preview" class="p-4 bg-base-50 dark:bg-white/5 rounded-xl">
    <div class="grid gap-4 lg:grid-cols-3">
      <Button variant="accent" gap="xs">
        <Plus slot="left-icon" size="sm" />
        Launch project
      </Button>
      <Button variant="outline" gap="xs">
        Next step
        <ArrowRight slot="right-icon" size="sm" />
      </Button>
      <div class="flex items-center gap-3">
        <Button variant="icon" iconOnly aria-label="Search">
          <SearchIcon slot="left-icon" size="sm" />
        </Button>
        <Button variant="onlyIcon" iconOnly aria-label="Alerts">
          <AlertTriangle slot="left-icon" size="sm" />
        </Button>
      </div>
    </div>
  </TabPanel>

  <TabPanel slot="content" tab="code">
```astro
<Button variant="accent" gap="xs">
  <Plus slot="left-icon" size="sm" />
  Launch project
</Button>

<Button variant="outline" gap="xs">
  Next step
  <ArrowRight slot="right-icon" size="sm" />
</Button>

<Button variant="icon" iconOnly aria-label="Search">
  <SearchIcon slot="left-icon" size="sm" />
</Button>

<Button variant="onlyIcon" iconOnly aria-label="Alerts">
  <AlertTriangle slot="left-icon" size="sm" />
</Button>
```
  </TabPanel>
</Tabs>

### State helpers

<Tabs defaultTab="preview">
  <TabButton slot="tabs" tab="preview">Preview</TabButton>
  <TabButton slot="tabs" tab="code">Code</TabButton>

  <TabPanel slot="content" tab="preview" class="p-4 bg-base-50 dark:bg-white/5 rounded-xl">
    <div class="space-y-4">
      <div class="flex flex-wrap items-center gap-3">
        <Button loading variant="outline">Saving…</Button>
        <Button disabled variant="ghost">Disabled</Button>
        <Button variant="accent" iconOnly aria-label="Add">
          <Plus slot="left-icon" size="sm" />
        </Button>
      </div>
      <Button fullWidth variant="default">Continue</Button>
    </div>
  </TabPanel>

  <TabPanel slot="content" tab="code">
```astro
<Button loading variant="outline">Saving…</Button>
<Button disabled variant="ghost">Disabled</Button>
<Button variant="accent" iconOnly aria-label="Add">
  <Plus slot="left-icon" size="sm" />
</Button>

<Button fullWidth variant="default">Continue</Button>
```
  </TabPanel>
</Tabs>

### Rounding helpers

<Tabs defaultTab="preview">
  <TabButton slot="tabs" tab="preview">Preview</TabButton>
  <TabButton slot="tabs" tab="code">Code</TabButton>

  <TabPanel slot="content" tab="preview" class="p-4 bg-base-50 dark:bg-white/5 rounded-xl">
    <div class="flex flex-wrap gap-3">
      <Button variant="outline" noRounded>noRounded</Button>
      <Button variant="outline" noRoundedLeft>noRoundedLeft</Button>
      <Button variant="outline" noRoundedRight>noRoundedRight</Button>
      <Button variant="outline" noRoundedTop>noRoundedTop</Button>
      <Button variant="outline" noRoundedBottom>noRoundedBottom</Button>
    </div>
  </TabPanel>

  <TabPanel slot="content" tab="code">
```astro
<Button variant="outline" noRounded>noRounded</Button>
<Button variant="outline" noRoundedLeft>noRoundedLeft</Button>
<Button variant="outline" noRoundedRight>noRoundedRight</Button>
<Button variant="outline" noRoundedTop>noRoundedTop</Button>
<Button variant="outline" noRoundedBottom>noRoundedBottom</Button>
```
  </TabPanel>
</Tabs>

