---
import Text from "@/components/fundations/typography/Text.astro";

const {
  class: className,
  showLink = false,
  href = "#_",
  linkText = "Download",
} = Astro.props;
---

<Text
  tag="p"
  variant="textXS"
  class={`relative font-semibold uppercase tracking-wide text-muted-foreground ${className ?? ""}`}
>
  <slot />
  {
    showLink && (
      <>
        <a href={href} class="relative text-foreground">
          <span class="absolute inset-0" aria-hidden="true" />
          {linkText}
        </a>
      </>
    )
  }
</Text>
