---
import Text from "@/components/fundations/typography/Text.astro";

const {
  class: className,
  showLink = false,
  href = "#_",
  linkText = "Download",
} = Astro.props;
---

<Text
  tag="p"
  variant="textXS"
  class={`relative font-semibold uppercase tracking-wide text-base-500 ${className ?? ""}`}
>
  <slot />
  {
    showLink && (
      <>
        <a href={href} class="relative text-primary/88  ">
          <span class="absolute inset-0" aria-hidden="true" />
          {linkText}
        </a>
      </>
    )
  }
</Text>
