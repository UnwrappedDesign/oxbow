<section>
  <div class="px-8 py-24 mx-auto md:px-12 lg:px-32 max-w-7xl relative">
    <div class="grid items-center grid-cols-1 gap-6 lg:grid-cols-2 lg:gap-24">
      <div class="md:order-first">
        <span
          class="font-mono text-xs font-medium tracking-tight text-blue-600 uppercase"
          >Your tagline</span
        >
        <h1
          class="text-4xl mt-4 font-semibold tracking-tight text-gray-900 lg:text-5xl lg:text-balance">
          Transform your visitors into

          <span>users with an engaging headline</span>
        </h1>
        <dl class="grid grid-cols-2 gap-4 mt-12 list-none lg:gap-6">
          <div>
            <dt class="text-gray-900 font-medium text-sm font-mono uppercase">
              No Guarantee
            </dt>
            <dd class="mt-2 text-sm text-gray-500">
              The recipe is provided "as is" without any guarantees. Bake at
              your own risk.
            </dd>
          </div>
          <div>
            <dt class="text-gray-900 font-medium text-sm font-mono uppercase">
              Commercial Baking Allowed
            </dt>
            <dd class="mt-2 text-sm text-gray-500">
              You may use the recipe for both home baking and commercial
              purposes.
            </dd>
          </div>
        </dl>
      </div>
      <div class="order-first block w-full mt-12 aspect-square lg:mt-0">
        <div
          x-data="{ activeSlide: 0, slides: ['/images/blob2.jpeg', '/images/blob1.jpeg', '/images/blob4.jpeg'] }"
          class="h-full relative"
          role="region"
          aria-label="Image Slider">
          <!-- Template for each slide image -->
          <template
            x-for="(slide, index) in slides"
            :key="index">
            <div
              x-show="activeSlide === index"
              x-transition:enter="transition ease-out duration-500"
              x-transition:enter-start="opacity-0 transform scale-95"
              x-transition:enter-end="opacity-100 transform scale-100"
              x-transition:leave="transition ease-in duration-500"
              x-transition:leave-start="opacity-100 transform scale-100"
              x-transition:leave-end="opacity-0 transform scale-95"
              class="h-full w-full absolute inset-0"
              :aria-hidden="activeSlide !== index">
              <!-- Slide image -->
              <img
                :src="slide"
                alt="#"
                class="w-full h-full object-cover rounded-2xl drop-shadow"
              />
            </div>
          </template>
          <!-- Previous slide button -->
          <button
            @click="activeSlide = activeSlide === 0 ? slides.length - 1 : activeSlide - 1"
            class="absolute left-4 top-1/2 transform -translate-y-1/2 bg-white border text-gray-900 rounded-full size-10 flex items-center justify-center focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-indigo-500"
            aria-label="Previous Slide">
            &larr;
          </button>
          <!-- Next slide button -->
          <button
            @click="activeSlide = activeSlide === slides.length - 1 ? 0 : activeSlide + 1"
            class="absolute right-4 top-1/2 transform -translate-y-1/2 bg-white border text-gray-900 rounded-full size-10 flex items-center justify-center focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-indigo-500"
            aria-label="Next Slide">
            &rarr;
          </button>
          <!-- Pagination indicators -->
          <div
            class="absolute bottom-4 left-1/2 transform -translate-x-1/2 flex space-x-2">
            <template
              x-for="(slide, index) in slides"
              :key="index">
              <button
                @click="activeSlide = index"
                :class="{'bg-gray-800': activeSlide === index, 'bg-gray-400': activeSlide !== index}"
                class="h-3 w-3 rounded-full focus:outline-none"
                :aria-label="'Slide ' + (index + 1)"
                :aria-current="activeSlide === index ? 'true' : 'false'"
              ></button>
            </template>
          </div>
        </div>
      </div>
    </div>
  </div>
</section>
