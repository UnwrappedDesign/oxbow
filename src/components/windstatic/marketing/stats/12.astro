<section>
  <div class="px-8 py-24 mx-auto md:px-12 lg:px-32 max-w-7xl relative">
    <div class="grid grid-cols-1 gap-12 lg:grid-cols-2 lg:items-start">
      <div>
        <span
          class="font-mono text-xs font-medium tracking-tight text-blue-600 uppercase"
          >Your tagline</span
        >
        <h1
          class="text-4xl mt-4 font-semibold tracking-tight text-gray-900 lg:text-balance">
          Empower your finances with our unmatched value
        </h1>
        <p class="text-sm font-medium mt-4 text-gray-500">
          Experience unbeatable low fees, unwavering commitment, and significant
          earnings potential. With over 60 million satisfied users globally, our
          trusted service is designed to maximize your financial growth while
          offering unparalleled support and security.

          <br>
          <br>
          Join a community that
          prioritizes your success, where every transaction counts towards a
          brighter financial future.
        </p>
      </div>
      <div class="flex flex-col gap-x-6 gap-y-12 lg:ml-auto">
        <div
          x-data="{ count: 0, target: 50, increment: 5, counter() { let interval = setInterval(() => { if (this.count < this.target) { this.count += this.increment; } else { this.count = this.target; clearInterval(interval); } }, 20); } }"
          x-intersect.once="counter()"
          class="flex flex-col">
          <dt
            class="mt-4 font-mono uppercase tracking-tighter text-sm font-medium text-gray-900">
            Working Individuals
          </dt>
          <dd
            x-text="`${count}+ Millions`"
            class="order-first text-5xl lg:text-6xl font-normal font-mono text-blue-600">
          </dd>
        </div>
        <div
          x-data="{ count: 0, target: 60, increment: 5, counter() { let interval = setInterval(() => { if (this.count < this.target) { this.count += this.increment; } else { this.count = this.target; clearInterval(interval); } }, 20); } }"
          x-intersect.once="counter()"
          class="flex flex-col">
          <dt
            class="mt-4 font-mono uppercase tracking-tighter text-sm font-medium text-gray-900">
            super Low Fee
          </dt>
          <dd
            x-text="`${count}+ Billion`"
            class="order-first text-5xl lg:text-6xl font-normal font-mono text-blue-600">
          </dd>
        </div>
      </div>
    </div>
  </div>
</section>
