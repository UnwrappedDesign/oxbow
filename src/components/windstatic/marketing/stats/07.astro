<section>
  <div class="px-8 py-24 mx-auto md:px-12 lg:px-32 max-w-7xl relative">
    <div class="max-w-2xl">
      <span
        class="font-mono text-xs font-medium tracking-tight text-blue-600 uppercase"
        >Your tagline</span
      >
      <h1
        class="text-4xl mt-4 font-semibold tracking-tight text-gray-900 lg:text-5xl sm:text-balance">
        Empower your finances with our unmatched value
      </h1>
      <p class="text-base font-medium mt-4 text-gray-500 sm:text-balance">
        Experience low fees, unwavering commitment, and massive earnings with a
        service trusted by over 60 million users.
      </p>
    </div>
    <div class="flex mt-8 flex-wrap items-center gap-2 lg:ml-auto">
      <button
        class="flex items-center justify-center h-10 px-4 py-2 text-sm font-semibold text-white transition-all duration-200 rounded-lg bg-gradient-to-b from-blue-500 to-indigo-600 hover:to-indigo-800 shadow focus:ring-2 focus:ring-blue-950 focus:ring-offset-2 ring-offset-gray-200 hover:shadow-none"
        aria-label="Action">
        Get started
      </button>
      <button
        class="flex items-center justify-center gap-2 h-10 px-4 py-2 text-sm font-semibold text-gray-500 transition-all duration-200 bg-gray-50 border border-gray-300 rounded-lg hover:text-blue-700 focus:ring-2 shadow shadow-gray-500/5 focus:ring-blue-950 focus:ring-offset-2 ring-offset-gray-200 hover:shadow-none"
        aria-label="Action">
        Lerarn more
        <svg
          xmlns="http://www.w3.org/2000/svg"
          width="18"
          height="18"
          viewBox="0 0 24 24"
          fill="none"
          stroke="currentColor"
          stroke-width="2"
          stroke-linecap="round"
          stroke-linejoin="round"
          class="icon icon-tabler icons-tabler-outline icon-tabler-chevron-right">
          <path
            stroke="none"
            d="M0 0h24v24H0z"
            fill="none"
          ></path>
          <path d="M9 6l6 6l-6 6"></path>
        </svg>
      </button>
    </div>

    <div
      class="grid grid-cols-1 md:grid-cols-3 mt-12 lg:-mt-16 gap-6 items-end">
      <div
        x-data="{ count: 0, target: 80, counter() { let interval = setInterval(() => { if (this.count < this.target) { this.count += 1; } else { clearInterval(interval); } }, 50); } }"
        x-intersect.once="counter()"
        class="flex justify-between rounded-3xl bg-gray-50 flex-col lg:gap-12">
        <span
          x-text="`${count}%`"
          class="text-5xl font-normal font-mono text-blue-600 p-8 order-last border-t w-full">
        </span>
        <div class="sm:shrink lg:flex-none p-8">
          <p
            class="font-mono uppercase tracking-tighter text-sm font-medium text-gray-900">
            Working Individuals
          </p>
          <p class="mt-2 text-sm text-gray-500 text-pretty">
            We’re dedicated to delivering outstanding service and value.
          </p>
        </div>
      </div>
      <div
        x-data="{ count: 0, target: 50, counter() { let interval = setInterval(() => { if (this.count < this.target) { this.count += 1; } else { clearInterval(interval); } }, 50); } }"
        x-intersect.once="counter()"
        class="flex justify-between rounded-3xl bg-blue-600 flex-col lg:gap-28">
        <span
          x-text="`${count}B`"
          class="text-5xl font-normal font-mono text-white p-8 order-last border-t w-full border-white/20">
        </span>
        <div class="sm:shrink lg:flex-none p-8">
          <p
            class="font-mono uppercase tracking-tighter text-sm font-medium text-white">
            Massive Earnings
          </p>
          <p class="text-sm text-gray-200 text-pretty mt-2">
            Reap the benefits with substantial returns .
          </p>
        </div>
      </div>
      <div
        x-data="{ count: 0, target: 40, counter() { let interval = setInterval(() => { if (this.count < this.target) { this.count += 1; } else { clearInterval(interval); } }, 50); } }"
        x-intersect.once="counter()"
        class="flex justify-between rounded-3xl bg-blue-950 flex-col lg:gap-44">
        <span
          x-text="`${count}M`"
          class="text-5xl font-normal font-mono text-white p-8 order-last border-t border-white/20 w-full">
        </span>
        <div class="sm:shrink lg:flex-none p-8">
          <p
            class="font-mono uppercase tracking-tighter text-sm font-medium text-white">
            super Low Fee
          </p>
          <p class="text-sm text-gray-200 text-pretty mt-2">
            Enjoy significant savings with our extremely low fee structure.
          </p>
        </div>
      </div>
    </div>
  </div>
</section>
