---
export const freeComponent = true;

import Text from "@/components/fundations/typography/Text.astro";
const steps = [
  {
    title: "Select Your Account",
    description: "Pick a checking, savings, or both to match your needs.",
  },
  {
    title: "Prepare Your Documents",
    description: "Have your ID, proof of address, and SSN ready.",
  },
  {
    title: "Submit Your Application",
    description: "Fill out and submit your application accurately.",
  },
];
---

<section class="bg-background">
  <div class="px-8 py-24 mx-auto max-w-7xl md:px-12 lg:px-20">
    <dl
      class="grid grid-cols-1 gap-x-4 gap-y-12 lg:grid-cols-3 lg:divide-x lg:border-x border-border divide-border"
    >
      {
        steps.map((step, index) => (
          <div class="lg:px-8">
            <dt class="flex items-center gap-3">
              <Text
                tag="h3"
                variant="textBase"
                class="flex items-center font-medium text-foreground gap-2"
              >
                <span class="text-foreground">
                  {index + 1}.
                </span>
                {step.title}
              </Text>
            </dt>
            <dd class="mt-2">
              <Text
                tag="p"
                variant="textBase"
                class="font-medium text-muted-foreground"
              >
                {step.description}
              </Text>
            </dd>
          </div>
        ))
      }
    </dl>
  </div>
</section>
