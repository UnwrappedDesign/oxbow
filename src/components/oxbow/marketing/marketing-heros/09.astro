---
export const description = "Simple with center text and image";
import { Image } from "astro:assets";
import DashboardLight from "@/images/assets/dashboardLight.svg";
// Fundations
import Text from "@/components/fundations/typography/Text.astro";
import Kicker from "@/components/fundations/typography/Kicker.astro";
import Button from "@/components/fundations/buttons/Button.astro";
const stats = [
  {
    label: "Working Individuals",
    value: "30%",
  },
  {
    label: "Super low fee",
    value: "40%",
  },
  {
    label: "Great Commitment",
    value: "50%",
  },
  {
    label: "Massive Earnings",
    value: "60M",
  },
];
---

<section class="overflow-hidden bg-background lg:p-8">
  <div
    class="w-full px-8 py-20 pb-0 mx-auto overflow-hidden md:px-12 lg:px-42 2xl:max-w-7xl bg-linear-45 from-muted to-background lg:rounded-xl"
  >
    <div class="max-w-xl">
      <Kicker
        showLink={true}
        href="#_"
        linkText="See all features"
        class="text-foreground"
      >
        Less busywork, more results
      </Kicker>
      <Text
        tag="h2"
        variant="displayMD"
        class="mt-12 font-semibold tracking-tight text-foreground lg:text-balance"
      >
        Collaboration that just works
      </Text>
      <Text
        tag="p"
        variant="textSM"
        class="mt-4 text-muted-foreground lg:text-balance"
      >
        Launch projects, invite your team, and keep everything in sync—without
        the headaches.
      </Text>

      <div class="flex flex-wrap items-center mt-8 gap-2">
        <Button variant="accent" size="sm">Start now</Button>
        <Button variant="muted" size="sm">See pricing</Button>
      </div>
    </div>
    <div class="mt-12 grid grid-cols-2 gap-4 sm:grid-cols-2 md:grid-cols-4">
      {
        stats.map((stat) => (
          <div class="flex flex-col">
            <Text
              tag="p"
              variant="textBase"
              class="font-medium text-foreground"
            >
              {stat.value}
            </Text>
            <Text
              tag="h3"
              variant="textSM"
              class="font-medium text-muted-foreground"
            >
              {stat.label}
            </Text>
          </div>
        ))
      }
    </div>
    <div
      class="relative mt-4 overflow-hidden rounded-t-lg shadow outline outline-border"
    >
      <Image
        width={1200}
        height={1200}
        loading="eager"
        decoding="async"
        alt="dashboard"
        src={DashboardLight}
        class="object-cover h-full -mb-12"
      />
    </div>
  </div>
</section>
