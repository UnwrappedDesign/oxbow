---
export const description = "Splited CTA with text-and image";
// Assets
import { Image } from "astro:assets";
import Activity from "@/images/assets/activity.svg";
// Foundations
import Text from "@/components/fundations/typography/Text.astro";
import Button from "@/components/fundations/buttons/Button.astro";
import Wrapper from "@/components/fundations/containers/Wrapper.astro";
// Icons
import Check from "@/components/fundations/icons/Check.astro";
---

<section>
  <Wrapper variant="blockStandard" class="py-32">
    <div
      class="max-w-md mx-auto grid grid-cols-1 gap-12 lg:gap-24 lg:max-w-none md:grid-cols-2 items-center"
    >
      <div class="lg:text-balance max-w-xl lg:order-last">
        <Text
          tag="h2"
          variant="displayLG"
          class="font-semibold tracking-tight text-base-900 lg:text-balance"
        >
          Organize your chaos. Or at least make it look like you did
        </Text>
        <Text
          tag="p"
          variant="textSM"
          class="mt-4 text-base-500 lg:text-balance"
        >
          Our SaaS doesn’t promise to fix your life. But it *will* make it look
          like you’ve got things under control. That’s half the battle, right?
        </Text>

        <div class="flex flex-col gap-4 mt-8">
          <div class="flex items-center gap-2 text-base-600">
            <div
              class="bg-base-50 outline outline-base-100 text-accent-500 p-2 rounded-lg"
            >
              <Check size="sm" />
            </div>

            <Text tag="p" variant="textSM">
              Dashboards your boss will nod at during meetings — and then ask
              you to explain in Slack anyway.
            </Text>
          </div>
          <div class="flex items-center gap-2 text-base-600">
            <div
              class="bg-base-50 outline outline-base-100 text-accent-500 p-2 rounded-lg"
            >
              <Check size="sm" />
            </div>
            <Text tag="p" variant="textSM">
              Tasks that auto-sort themselves because, let’s face it, you're too
              busy doomscrolling to do it manually.
            </Text>
          </div>
          <div class="flex items-center gap-2 text-base-600">
            <div
              class="bg-base-50 outline outline-base-100 text-accent-500 p-2 rounded-lg"
            >
              <Check size="sm" />
            </div>
            <Text tag="p" variant="textSM">
              Notifications that give you what you need to know — without
              triggering your fight-or-flight response.
            </Text>
          </div>
          <div class="flex items-center gap-2 text-base-600">
            <div
              class="bg-base-50 outline outline-base-100 text-accent-500 p-2 rounded-lg"
            >
              <Check size="sm" />
            </div>
            <Text tag="p" variant="textSM">
              A UI so clean you’ll wonder if it’s hiding something. (Spoiler:
              it’s just well-designed.)
            </Text>
          </div>
        </div>

        <div class="flex flex-wrap gap-2 mt-12">
          <Button variant="default" size="sm"> Sign Me Up, I Guess </Button>
          <Button variant="muted" size="sm"> I'll Keep Winging It </Button>
        </div>
      </div>

      <div class="h-full">
        <Image
          src={Activity}
          width={600}
          height={800}
          alt="Product showcase"
          loading="eager"
          decoding="async"
          class="w-full shadow rounded-lg outline outline-base-200"
        />
      </div>
    </div>
  </Wrapper>
</section>
