---
import Button from "@/components/fundations/buttons/Button.astro";
import Text from "@/components/fundations/typography/Text.astro";
import Filters from "@/components/fundations/icons/Filters.astro";
import WaveSquare from "@/components/fundations/icons/WaveSquare.astro";
import ArrowIteration from "@/components/fundations/icons/ArrowIteration.astro";
import Plus from "@/components/fundations/icons/Plus.astro";
const data = [
  {
    icon: "Filters",
    category: "Live editing",
    text: "Instantly see the result of every change you make.",
  },
  {
    icon: "WaveSquare",
    category: "Autocompletion",
    text: "Spotless will suggest the right classes for you as you type.",
  },
  {
    icon: "ArrowIteration",
    category: "Hide/show classes",
    text: "Hide or show classes to see how your design changes.",
  },
  {
    icon: "Plus",
    category: "Color preview",
    text: "See the color of every class in the autocompletion view.",
  },
];
---

<section>
  <div class="px-8 py-32 mx-auto md:px-12 lg:px-24 max-w-7xl relative">
    <div class="grid grid-cols-1 lg:grid-cols-2 gap-12 items-start">
      <div>
        <Text
          tag="p"
          variant="textSM"
          class="font-bold uppercase text-accent-600">
          Tagline
        </Text>
        <Text
          tag="h2"
          variant="displayMD"
          class="mt-4 font-semibold text-base-900 lg:text-balance">
          Transforming the banking experience for a digital future
        </Text>
        <Text
          tag="p"
          variant="textBase"
          class="mt-4 text-base-500 font-medium">
          The fastest method for working together on staging and temporary
          environments.
        </Text>
        <div class="flex items-center gap-4 mt-8 flex-row">
          <Button
            variant="accent"
            size="sm">
            Get started
          </Button>
          <div class="text-sm text-base-500 font-medium lg:items-center">
            <Text
              tag="span"
              class="block"
              variant="textMD">
              This is some support text
            </Text>
            <Text
              tag="span"
              class="block"
              variant="textMD">
              and this is also.
            </Text>
          </div>
        </div>
      </div>
      <dl class="grid grid-cols-1 lg:grid-cols-2 gap-10">
        {
          data.map((section) => (
            <div>
              <div class="text-accent-500 flex gap-3 items-center">
                {section.icon === "Filters" && (
                  <Filters
                    class="text-accent-600"
                    size="base"
                  />
                )}
                {section.icon === "WaveSquare" && (
                  <WaveSquare
                    class="text-accent-600"
                    size="base"
                  />
                )}
                {section.icon === "ArrowIteration" && (
                  <ArrowIteration
                    class="text-accent-600"
                    size="base"
                  />
                )}
                {section.icon === "Plus" && (
                  <Plus
                    class="text-accent-600"
                    size="base"
                  />
                )}
                <dt>
                 <Text
                  tag="h3"
                  variant="textLG"
                  class=" text-base-900 font-medium">
                    {section.category}
                  </Text>
                </dt>
              </div>
              <dd>
                <Text
                  tag="p"
                  variant="textBase"
                  class="mt-4 text-base-500 font-medium">
                  {section.text}
                </Text>
              </dd>
            </div>
          ))
        }
      </dl>
    </div>
  </div>
</section>
