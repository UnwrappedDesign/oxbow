---
export const freeComponent = true;
export const excludeFromObfuscation = ["flex", "hidden"];
import IconButton from "@/components/fundations/buttons/IconButton.astro";
import Burger from "@/components/fundations/icons/Burger.astro";
import Button from "@/components/fundations/buttons/Button.astro";
import Logo from "@/components/assets/Logo.astro";
import Text from "@/components/fundations/typography/Text.astro";
import ChevronDown from "@/components/fundations/icons/ChevronDown.astro";
import FlyoutLinkBox from "@/components/fundations/wrappers/FlyoutLinkBox.astro";
import Plus from "@/components/fundations/icons/Plus.astro";
---

<div class="bg-background pb-100">
  <!-- copy:start -->
  <header
    class="w-full bg-background border-b border-border"
  >
    <div
      x-data="{ open:false, cats:false }"
      class="px-6 mx-auto max-w-screen-xl md:px-10 lg:px-16"
    >
      <div class="flex items-center justify-between h-16 gap-3">
        <a href="#_" class="inline-flex items-center gap-2"
          ><Logo class="text-foreground h-7" /><span class="sr-only"
            >Home</span
          ></a
        >
        <div class="items-center hidden md:flex gap-2">
          <div x-data="{open:false}" class="relative">
            <Button
              size="xs"
              variant="ghost"
              @click="open=!open"
              @keydown.escape.window="open=false"
              :aria-expanded="open"
              class="flex items-center justify-between gap-2"
              >Categories
              <ChevronDown
                size="sm"
                slot="right-icon"
                :class="open ? '-rotate-180' : ''"
                class="duration-300 ease-out transform"
              />
            </Button>
            <div
              x-show="open"
              x-transition
              @click.outside="open=false"
              class="absolute left-0 top-full z-30 mt-2 w-[680px] rounded-xl border border-border bg-background shadow-subtle p-4 grid grid-cols-3 gap-3"
            >
              <FlyoutLinkBox
                href="#_"
                title="Men"
                description="Clothing & accessories"
              />
              <FlyoutLinkBox
                href="#_"
                title="Women"
                description="Latest arrivals"
              />
              <FlyoutLinkBox href="#_" title="Kids" description="New season" />
              <FlyoutLinkBox
                href="#_"
                title="Home"
                description="Decor & furniture"
              />
              <FlyoutLinkBox
                href="#_"
                title="Electronics"
                description="Gadgets & audio"
              />
              <FlyoutLinkBox
                href="#_"
                title="Sports"
                description="Outdoor & fitness"
              />
            </div>
          </div>
          <Text
            tag="a"
            href="#_"
            variant="textSM"
            class="font-medium text-muted-foreground hover:text-foreground"
            >Deals</Text
          >
          <Text
            tag="a"
            href="#_"
            variant="textSM"
            class="font-medium text-muted-foreground hover:text-foreground"
            >New</Text
          >
        </div>
        <div class="items-center hidden md:flex gap-2">
          <Button variant="muted" size="xs">Cart (2)</Button>
          <Button variant="accent" size="xs">Checkout</Button>
        </div>
        <IconButton
          class="md:hidden"
          @click="open=!open"
          :aria-expanded="open"
          aria-controls="nav-07"
          variant="muted"
          size="xs"
        >
          <Burger size="xs" slot="icon" />
        </IconButton>
      </div>
      <div
        id="nav-07"
        :class="{ 'grid': open, 'hidden': !open }"
        class="hidden pb-4 grid-cols-1 gap-2 md:hidden"
      >
        <details>
          <summary
            class="items-start justify-between w-full text-sm font-medium text-left select-none grid grid-cols-4 text-foreground hover:text-muted-foreground focus:text-muted-foreground gap-4focus:text-muted-foreground"
            ><span class="col-span-3">Categories</span>
            <Plus
              class="ml-auto duration-300 ease-out transform group-open:-rotate-45 group-open:text-foreground text-muted-foreground"
              size="sm"
            />
          </summary>
          <div
            class="py-2 pt-2 text-sm text-muted-foreground space-y-2"
          >
            <Text
              tag="p"
              variant="textSM"
              class="font-medium text-muted-foreground">Men</Text
            >
            <Text
              tag="p"
              variant="textSM"
              class="font-medium text-muted-foreground">Women</Text
            >
            <Text
              tag="p"
              variant="textSM"
              class="font-medium text-muted-foreground">Kids</Text
            >
            <Text
              tag="p"
              variant="textSM"
              class="font-medium text-muted-foreground">Home</Text
            >
            <Text
              tag="p"
              variant="textSM"
              class="font-medium text-muted-foreground"
              >Electronics</Text
            >
            <Text
              tag="p"
              variant="textSM"
              class="font-medium text-muted-foreground">Sports</Text
            >
          </div>
        </details>

        <Button variant="muted" size="xs">Deals</Button>
        <Button variant="muted" size="xs">NEw</Button>
        <Button variant="muted" size="xs">Cart (2)</Button>
        <Button variant="accent" size="xs">Checkout</Button>
      </div>
    </div>
  </header>
  <!-- copy:end -->
</div>
