---
export const excludeFromObfuscation = ["ring-destructive", "focus:ring-destructive"];

export const description = "Textarea with Character Highlight";
import Text from "@/components/fundations/typography/Text.astro";
---

<div class="flex flex-col justify-center p-8 mx-auto bg-background">
  <div class="w-full max-w-sm mx-auto">
    <!-- copy:start -->
    <div class="w-full space-y-1" x-data="{ text: '', threshold: 50 }">
      <Text
        for="highlight-textarea"
        tag="label"
        variant="textSM"
        class="font-medium text-muted-foreground"
      >
        Highlight excess characters
      </Text>
      <textarea
        id="highlight-textarea"
        x-model="text"
        rows="4"
        placeholder="Type your message"
        class="block w-full px-4 py-2 text-sm text-foreground bg-background border border-transparent rounded-lg appearance-none duration-300 ring-1 ring-border placeholder:text-muted-foreground focus:border-border focus:bg-transparent focus:outline-none focus:ring-border focus:ring-offset-2 focus:ring-2 sm:text-sm"
        :class="{ 'ring-destructive focus:ring-destructive': text.length > threshold }"
      ></textarea>
      <div class="mt-2 text-sm text-muted-foreground">
        Characters: <span x-text="text.length"></span> (Limit: <span
          x-text="threshold"></span>)
      </div>
    </div>
    <!-- copy:end -->
</div>
</div>
