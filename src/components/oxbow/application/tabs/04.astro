<section>
  <div class="relative px-8 py-12 mx-auto md:px-12 lg:px-24 max-w-7xl">
    <div x-data="{ tab: 'tab1' }">
      <div class="flex justify-center">
        <ul
          class="inline-flex gap-4 text-sm text-base-500 border border-base-200 w-auto p-2 rounded-xl">
          <li>
            <a
              @click.prevent="tab = 'tab1'"
              href="#_"
              class="flex items-center justify-center h-9 px-4 py-2 text-sm font-medium rounded-lg transition-all duration-200"
              :class="tab === 'tab1' ? 'bg-accent-700 text-white shadow-md' : 'bg-base-50 hover:text-accent-500'">
              Marketing
            </a>
          </li>
          <li>
            <a
              @click.prevent="tab = 'tab2'"
              href="#_"
              class="flex items-center justify-center h-9 px-4 py-2 text-sm font-medium rounded-lg transition-all duration-200"
              :class="tab === 'tab2' ? 'bg-accent-700 text-white shadow-md' : 'bg-base-50 hover:text-accent-500'">
              Application
            </a>
          </li>
          <li>
            <a
              @click.prevent="tab = 'tab3'"
              href="#_"
              class="flex items-center justify-center h-9 px-4 py-2 text-sm font-medium rounded-lg transition-all duration-200"
              :class="tab === 'tab3' ? 'bg-accent-700 text-white shadow-md' : 'bg-base-50 hover:text-accent-500'">
              ECommerce
            </a>
          </li>
        </ul>
      </div>
      <div class="py-4 mt-4 text-left bg-white content">
        <div
          x-show="tab==='tab1'"
          class="text-base-500">
          <main class="py-4">
            <p class="text-base-500 text-sm">Marketing content</p>
          </main>
        </div>
        <div
          x-show="tab==='tab2'"
          class="text-base-500"
          style="display: none;">
          <main class="py-4">
            <p class="text-base-500 text-sm">Application content</p>
          </main>
        </div>
        <div
          x-show="tab==='tab3'"
          class="text-base-500"
          style="display: none;">
          <main class="py-4">
            <p class="text-base-500 text-sm">ECommerce content</p>
          </main>
        </div>
      </div>
    </div>
  </div>
</section>
