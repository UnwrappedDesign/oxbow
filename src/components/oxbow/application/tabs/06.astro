---
export const excludeFromObfuscation = [
  "bg-muted",
  "text-muted-foreground",
  "ring-1",
  "ring-border",
  "bg-muted",
];
---

<div class="flex flex-col justify-center p-12 mx-auto bg-background">
  <!-- Accessible Tabs • Badges + subtle cards -->
  <div
    x-data="{
      keys: ['overview','projects','packages'],
      selected: 'overview',
      focus: 0,
      init(){ this.focus = 0 },
      select(id){ this.selected = id; this.focus = this.keys.indexOf(id) },
      onKeydown(e){
        const last = this.keys.length - 1
        let i = this.focus
        if (e.key==='ArrowRight' || e.key==='ArrowDown') i = i===last?0:i+1
        else if (e.key==='ArrowLeft' || e.key==='ArrowUp') i = i===0?last:i-1
        else if (e.key==='Home') i = 0
        else if (e.key==='End') i = last
        else return
        e.preventDefault(); const id = this.keys[i]; this.focus=i; this.$nextTick(()=>this.$refs['tab-'+id]?.focus())
      }
    }"
    class="max-w-2xl mx-auto"
  >
    <div role="tablist" aria-orientation="horizontal" class="flex gap-2">
      <template x-for="(id, idx) in keys" :key="id">
        <button
          class="inline-flex items-center px-3 text-sm font-medium gap-2 h-9 rounded-md focus:outline-none focus-visible:ring-2 focus-visible:ring-border"
          :id="'tab-'+id"
          role="tab"
          :aria-selected="selected===id"
          :tabindex="focus===idx?0:-1"
          :aria-controls="'panel-'+id"
          x-ref="tab-[[id]]"
          @click="select(id)"
          @keydown="onKeydown($event)"
          :class="selected===id ? ring-1 ring-border' : 'bg-background text-foreground hover:text-foreground'"
        >
          <span
            x-text="id==='overview' ? 'Overview' : id==='projects' ? 'Projects' : 'Packages'"
          ></span>
          <span
            x-show="id==='packages'"
            class="ml-1 inline-flex items-center px-1.5 h-5 text-[0.70rem] rounded-full bg-muted/50 text-foreground"
            >New</span
          >
          <span
            x-show="id==='projects'"
            class="ml-1 inline-flex items-center px-1.5 h-5 text-[0.70rem] rounded-full bg-background text-foreground border border-border"
            >3</span
          >
        </button>
      </template>
    </div>
    <div class="pt-4 text-sm text-muted-foreground">
      <section
        x-show="selected==='overview'"
        x-cloak
        role="tabpanel"
        :id="'panel-overview'"
        aria-labelledby="tab-overview"
      >
        <p>Overview content — announcements and latest changes.</p>
      </section>
      <section
        x-show="selected==='projects'"
        x-cloak
        role="tabpanel"
        :id="'panel-projects'"
        aria-labelledby="tab-projects"
      >
        <p>Projects content — ownership, contributors, and reports.</p>
      </section>
      <section
        x-show="selected==='packages'"
        x-cloak
        role="tabpanel"
        :id="'panel-packages'"
        aria-labelledby="tab-packages"
      >
        <p>Packages content — release notes, channels, and approvals.</p>
      </section>
    </div>
  </div>
</div>
