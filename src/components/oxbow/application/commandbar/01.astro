---
export const freeComponent = true;
import CommandbarButton from "@/components/fundations/buttons/CommandBarButton.astro";
import Component from "@/components/fundations/icons/Component.astro";
import Plus from "@/components/fundations/icons/Plus.astro";
import Search from "@/components/fundations/icons/Search.astro";
---

<section class="overflow-hidden bg-gray-50">
  <div class="relative px-8 py-32 mx-auto md:px-12 lg:px-24 max-w-7xl">
    <div
      class="max-w-xl p-3 mx-auto overflow-hidden bg-white shadow-lg transition-all transform rounded-xl"
    >
      <!-- Input section with label and accessible search -->
      <div class="relative bg-gray-50 focus-within:bg-white rounded-md">
        <label for="search" class="sr-only">Search</label>
        <Search
          class="pointer-events-none absolute top-3.5 left-4 text-gray-400"
          size="base"
          slot="icon"
        />
        <input
          id="search"
          type="text"
          class="w-full h-12 pr-4 bg-transparent border-0 text-gray-800 placeholder-gray-400 pl-11 focus:ring-0 sm:text-sm"
          placeholder="Search for anything"
          role="combobox"
          aria-expanded="false"
          aria-controls="options"
          aria-autocomplete="list"
        />
      </div>
      <!-- Options list with roles and keyboard focusability -->
      <ul
        class="pt-3 overflow-y-auto space-y-3 max-h-96 scroll-py-3"
        id="options"
        role="listbox"
        tabindex="0"
      >
        <li id="option-1" role="option" tabindex="0" aria-selected="false">
          <CommandbarButton
            text="Search projects..."
            hasKbd={true}
            kbdKey="S"
            iconSlot={true}
          >
            <Component size="sm" slot="icon" />
          </CommandbarButton>
        </li>
        <li id="option-2" role="option" tabindex="0" aria-selected="false">
          <CommandbarButton
            text="Create another project"
            hasKbd={true}
            kbdKey="C"
            iconSlot={true}
          >
            <Plus size="sm" slot="icon" />
          </CommandbarButton>
        </li>
        <!-- Add more items similarly with roles and attributes... -->
      </ul>
    </div>
  </div>
</section>
