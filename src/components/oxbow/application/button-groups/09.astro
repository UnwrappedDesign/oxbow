---
export const description = "Sign in Split Button (QR Modal)";
import HorizontalButtonGroup from "@/components/fundations/buttonGroups/HorizontalButtonGroup.astro";
import Qr from "@/components/fundations/icons/Apps.astro";
import Button from "@/components/fundations/buttons/Button.astro";
import { Image } from "astro:assets";
import qrCode from "@/images/assets/qrcode.png";
---

<section
  class="flex items-center justify-center bg-white p-18 py-42 dark:bg-zinc-900"
>
  <!-- copy:start -->
  <div
    class="inline-flex isolate"
    role="group"
    aria-label="Sign in"
    x-data="{ open:false }"
  >
    <HorizontalButtonGroup
      variant="default"
      position="right"
      size="sm"
      aria-label="Open QR sign-in"
      @click="open=true"
    >
      <Qr size="sm" slot="centered-icon" />
    </HorizontalButtonGroup>
    <HorizontalButtonGroup variant="default" position="left" size="sm">
      Sign in
    </HorizontalButtonGroup>

    <!-- Modal -->
    <div
      x-show="open"
      class="fixed inset-0 z-50 flex items-center justify-center p-42"
      style="display:none"
    >
      <div
        class="absolute inset-0 bg-black/40"
        @click="open=false"
        aria-hidden="true"
      >
      </div>
      <div
        role="dialog"
        aria-modal="true"
        class="relative p-4 bg-white shadow-xl w-80 w-fit rounded-xl dark:bg-zinc-900 ring-1 ring-zinc-200 dark:ring-zinc-700"
      >
        <h3 class="mb-2 text-sm font-semibold text-zinc-900 dark:text-white">
          Scan to sign in
        </h3>

        <Image
          width={500}
          height={500}
          src={qrCode}
          alt="QR code for lexingtonthemes.com"
          class="object-contain p-2 size-42"
        />
        <div class="flex justify-end mt-3">
          <Button size="xs" variant="outline" @click="open=false">Close</Button>
        </div>
      </div>
    </div>
  </div>
  <!-- copy:end -->
</section>
