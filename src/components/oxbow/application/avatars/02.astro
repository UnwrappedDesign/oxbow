<div class="flex items-center justify-center p-12 relative bg-white dark:bg-zinc-900">
  <!-- copy:start -->
  <div class="relative inline-flex items-center gap-3" x-data="{ online: true }">
    <span class="relative inline-block">
      <img class="rounded-full size-16 object-cover ring-2 ring-white dark:ring-zinc-800" src="/avatars/1.jpg" alt="User" />
      <span :class="online ? 'bg-emerald-500' : 'bg-zinc-400'" class="absolute top-0 right-0 block rounded-full size-4 ring-2 ring-white dark:ring-zinc-800"></span>
    </span>
    <button type="button" @click="online = !online" class="text-xs px-2 h-7 rounded border border-zinc-300 dark:border-zinc-700 text-zinc-600 dark:text-zinc-300">Toggle</button>
  </div>
  <!-- copy:end -->
</div>
