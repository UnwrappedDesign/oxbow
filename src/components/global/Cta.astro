---
import Text from "@/components/fundations/typography/Text.astro";
import Wrapper from "@/components/fundations/containers/Wrapper.astro";

// Check if the current page is the index page
const isIndexPage = Astro.url.pathname === "/";
---
{
      !isIndexPage && (
      
      <section class="border-t border-base-200">
        <!-- Conditionally render this section based on the page -->
        <Wrapper variant="standard" class="py-12 space-y-12">
        <div class="relative p-10 lg:p-10 bg-accent-600 overflow-hidden ring-4 ring-base-100 rounded-xl border border-base-200">
          <img
            class="invert absolute opacity-10 lg:-right-64 -top-12"
            src="/images/oxbow/windLogoDark.png"
            alt="#_"
          />
          <div class="relative">
            <Text
              tag="p"
              variant="textXS"
              class="font-bold mt-4 uppercase text-white"
            >
              Get acces
            </Text>
            <Text
              tag="h3"
              variant="displayLG"
              class="mt-4 text-white font-semibold text-balance"
            >
              Get lifetime access to all components, elements, and layouts for
              only <del class="opacity-45">$149</del> $67
            </Text>
            <Text
              tag="p"
              variant="textXL"
              class="mt-2 text-base-200 font-medium text-balance max-w-xl"
            >
              Own them forever with lifetime updates, unlimited usage, and
              support—no subscription required!
            </Text>

            <div class="flex mt-8">
              <Text
                tag="a"
                href="/pricing"
                aria-label="Pricing"
                title="Get full access"
                id="access-footer-cta"
                data-umami-event="access-footer-cta"
                class="flex items-center justify-center w-full sm:w-auto ring-4 gap-2 ring-base-50/20  h-12 px-6 py-2 font-medium text-accent-600 transition-all duration-200 rounded-full   focus:ring-2 focus:ring-offset-2 focus:outline-none bg-white border border-base-200 hover:text-accent-950 focus:ring-accent-600"
              >
                Get access for
                <span>
                  {" "}
                  <del class="opacity-45">$149</del> $67
                </span>
              </Text>
            </div>
          </div>
        </div>
    </Wrapper>
  </section>
      )
    }
